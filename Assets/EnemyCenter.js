#pragma strict

var speed:Vector3;

var count:int;
var vec:float;

var deg:float;
var count2:int;



function Start () {
    
    count2=0;

    deg=1;

    SetRandom();


     vec=-1;
}

function Update () {
/*
    if(--count<=0)
    {
        SetRandom();

        var start : int;
        var end : int;
        start = -1;
        end = 1;
        vec = Random.Range(start, end);
       
  }
  count2+= vec;

  transform.localRotation=Quaternion.AngleAxis(deg*count2,Vector3(0,0,1));

   var player= GameObject.FindWithTag("Player");

  transform.position=Vector3.SmoothDamp(transform.position,player.transform.position,speed,0.5);

  */
}

function SetRandom()
{
/*
    var start : int;
    var end : int;
    start = 0;
    end = 1000;
    count = Random.Range(start, end);
    */
}